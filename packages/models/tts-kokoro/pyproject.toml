[project]
name = "tts-kokoro"
version = "0.1.0"
description = "Kokoro-82M TTS inference package (isolated venv)"
requires-python = ">=3.11"
dependencies = [
    "tts-inference",
    "kokoro>=0.9.2",
    "torch>=2.0.0",
    "soundfile>=0.12.0",
    "loguru>=0.7.0",
    "pydantic>=2.5.0",
    "numpy>=1.26.0",
]

[tool.uv.sources]
tts-inference = { path = "../../tts-inference", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tts_kokoro"]
