kokoro>=0.9.2
# Pinned to match the Dockerfile's cu126 install, which runs first and is the
# authoritative one -- a floor here would let this second pip pass silently upgrade
# past it. See the Dockerfile for why the version is fixed at all.
torch==2.13.0
misaki[en]>=0.7.0
numpy>=2.0.2
soundfile>=0.12.0
lameenc>=1.8.0
soxr>=0.5.0
starlette>=0.37.0
uvicorn[standard]>=0.27.0
