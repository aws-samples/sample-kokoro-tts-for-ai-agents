[project]
name = "stt-tts-model-eval"
version = "0.1.0"
description = "Deploying and autoscaling the Kokoro-82M TTS model on Amazon SageMaker"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34.0",
    "pydantic>=2.5.0",
    "shared",
    "strands-agents>=1.50.2",
    "tts-inference",
]

[tool.uv.sources]
shared = { workspace = true }
tts-inference = { workspace = true }
tts-client = { workspace = true }
tts-eval = { workspace = true }
tts-bench = { workspace = true }
speech-infra = { workspace = true }
agent-infra = { workspace = true }

[tool.uv.workspace]
members = [
    "packages/shared",
    "packages/tts-inference",
    "packages/tts-client",
    "packages/tts-eval",
    "packages/tts-bench",
    "packages/speech-infra",
    "packages/agent-infra",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "pre-commit>=3.5.0",
    "ruff>=0.1.0",
    "mypy>=1.8.0",
    "shared",
    "tts-client",
    "tts-eval",
    "tts-bench",
    "speech-infra",
    "agent-infra",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = []

[tool.ruff.lint.pycodestyle]
max-line-length = 120

[tool.ruff.lint.isort]
known-first-party = [
    "shared",
    "tts_inference",
    "tts_client",
    "tts_eval",
    "tts_bench",
    "speech_infra",
    "agent_infra",
]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
strict_equality = true
exclude = ["packages/speech-infra/containers/", "packages/models/", "packages/agent-infra/container/"]

[tool.pytest.ini_options]
testpaths = [
    "packages/shared/tests",
    "packages/tts-inference/tests",
    "packages/tts-client/tests",
    "packages/tts-eval/tests",
    "packages/tts-bench/tests",
    "packages/speech-infra/tests",
    "packages/agent-infra/tests",
]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
addopts = ["-v", "--import-mode=importlib"]

[tool.uv]
package = false
